# =============================================================================
# dockerscout — Docker Management Platform
# =============================================================================
#
# Production-ready Docker Compose configuration.
#
# Quick start:
#   1. cp .env.example .env
#   2. Edit .env (change ALL passwords and secrets)
#   3. docker compose up -d
#   4. Open https://localhost:7443
#   5. Login with: admin / dockerscout
#
# With agent (multi-node):
#   docker compose --profile agent up -d
#
# Requirements:
#   - Docker Engine 24+ with Compose v2
#   - 2 GB RAM minimum (4 GB recommended)
#   - Ports 8080 (HTTP) and 7443 (HTTPS) available
#
# Image registries:
#   - Docker Hub:  dockerscout/dockerscout          (default)
#   - GHCR:        ghcr.io/dockerscout/dockerscout  (alternative)
#
# Network architecture:
#   - dockerscout-backend: Internal network for PostgreSQL, Redis, NATS (not exposed)
#   - dockerscout-frontend: Network for the application and web-facing services
#
# Documentation: https://docs.dockerscout.com
# Source:        https://github.com/fr4nsys/dockerscout
# License:       AGPL-3.0 (Community Edition)
#
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # dockerscout — Application Server
  # ---------------------------------------------------------------------------
  dockerscout:
    image: dockerscout/dockerscout:${DOCKERSCOUT_VERSION:-latest}
    container_name: dockerscout
    restart: unless-stopped
    ports:
      - "${DOCKERSCOUT_HTTP_PORT:-8080}:8080"    # HTTP
      - "${DOCKERSCOUT_HTTPS_PORT:-7443}:7443"   # HTTPS (auto-TLS)
    volumes:
      # Docker socket for container management (required)
      - /var/run/docker.sock:/var/run/docker.sock
      # Persistent application data (backups, PKI certs, license, etc.)
      - dockerscout_data:/app/data
      # Trivy vulnerability scanner cache
      - trivy_cache:/var/lib/dockerscout/trivy
    # Host terminal support (nsenter into host PID namespace)
    pid: "host"
    cap_add:
      - SYS_PTRACE    # For host process inspection
      - SYS_ADMIN      # For nsenter into host namespaces
    environment:
      # --- Operation mode ---
      # standalone: Single host, all services local
      # master: Multi-host mode, accepts agent connections
      - DOCKERSCOUT_MODE=${DOCKERSCOUT_MODE:-standalone}

      # --- Infrastructure connections ---
      - DOCKERSCOUT_DATABASE_URL=postgres://${DB_USER:-dockerscout}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-dockerscout}?sslmode=disable
      - DOCKERSCOUT_REDIS_URL=redis://redis:6379
      - DOCKERSCOUT_NATS_URL=nats://nats:4222

      # --- Security (MUST be changed in production) ---
      - DOCKERSCOUT_JWT_SECRET=${JWT_SECRET}
      - DOCKERSCOUT_ENCRYPTION_KEY=${ENCRYPTION_KEY}

      # --- Host terminal (web terminal into the Docker host) ---
      - HOST_TERMINAL_ENABLED=${HOST_TERMINAL_ENABLED:-true}
      - HOST_TERMINAL_USER=${HOST_TERMINAL_USER:-nobody}
      - HOST_TERMINAL_SHELL=/bin/bash

      # --- RDP Web Access (Apache Guacamole) ---
      - GUACD_ENABLED=${GUACD_ENABLED:-true}
      - GUACD_HOST=${GUACD_HOST:-guacd}
      - GUACD_PORT=${GUACD_PORT:-4822}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      nats:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "2.0"
        reservations:
          memory: 256M
          cpus: "0.25"
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    networks:
      - dockerscout-frontend
      - dockerscout-backend

  # ---------------------------------------------------------------------------
  # dockerscout Agent — Remote Node (optional, enable with --profile agent)
  # ---------------------------------------------------------------------------
  dockerscout-agent:
    image: dockerscout/dockerscout-agent:${DOCKERSCOUT_VERSION:-latest}
    container_name: dockerscout-agent
    restart: unless-stopped
    profiles:
      - agent
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agent_data:/app/data
    environment:
      - DOCKERSCOUT_GATEWAY_URL=nats://nats:4222
      - DOCKERSCOUT_AGENT_TOKEN=${AGENT_TOKEN:-change-me}
    depends_on:
      nats:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "test", "-f", "/app/data/agent.pid"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    networks:
      - dockerscout-backend

  # ---------------------------------------------------------------------------
  # Apache Guacamole Daemon — RDP/VNC Web Gateway
  # ---------------------------------------------------------------------------
  guacd:
    image: guacamole/guacd:1.5.5
    container_name: dockerscout-guacd
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4822 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    networks:
      - dockerscout-frontend

  # ---------------------------------------------------------------------------
  # PostgreSQL 16 — Primary Database
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: dockerscout-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-dockerscout}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-dockerscout}
      # Performance tuning for containerized PostgreSQL
      POSTGRES_INITDB_ARGS: "--data-checksums"
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=128MB"
      - "-c"
      - "work_mem=8MB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "effective_cache_size=256MB"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "wal_buffers=4MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "log_min_duration_statement=1000"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-dockerscout} -d ${DB_NAME:-dockerscout}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: "30m"
        max-file: "3"
    networks:
      - dockerscout-backend
    # Port NOT exposed to host by default for security.
    # Access via: docker exec -it dockerscout-postgres psql -U dockerscout

  # ---------------------------------------------------------------------------
  # Redis 8 — Sessions, Cache & JWT Blacklist
  # ---------------------------------------------------------------------------
  redis:
    image: redis:8-alpine
    container_name: dockerscout-redis
    restart: unless-stopped
    command:
      - "redis-server"
      - "--appendonly"
      - "yes"
      - "--maxmemory"
      - "256mb"
      - "--maxmemory-policy"
      - "allkeys-lru"
      - "--save"
      - "60"
      - "1000"
      - "--tcp-backlog"
      - "511"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 300M
          cpus: "0.5"
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - dockerscout-backend

  # ---------------------------------------------------------------------------
  # NATS 2.12 — Messaging & Agent Communication (JetStream)
  # ---------------------------------------------------------------------------
  nats:
    image: nats:2.12-alpine
    container_name: dockerscout-nats
    restart: unless-stopped
    command:
      - "-c"
      - "/etc/nats/nats-server.conf"
    volumes:
      - nats_data:/data
      - ./deploy/nats-server.conf:/etc/nats/nats-server.conf:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - dockerscout-backend

# =============================================================================
# Volumes — Named and persistent
# =============================================================================
volumes:
  dockerscout_data:
    name: dockerscout_data
  trivy_cache:
    name: dockerscout_trivy_cache
  postgres_data:
    name: dockerscout_postgres_data
  redis_data:
    name: dockerscout_redis_data
  nats_data:
    name: dockerscout_nats_data
  agent_data:
    name: dockerscout_agent_data

# =============================================================================
# Networks — Isolated backend and frontend
# =============================================================================
networks:
  # Backend network: PostgreSQL, Redis, NATS (not exposed to host)
  dockerscout-backend:
    name: dockerscout-backend
    driver: bridge
    internal: true
  # Frontend network: dockerscout app, guacd (web-facing services)
  dockerscout-frontend:
    name: dockerscout-frontend
    driver: bridge
