# Dynamic Reverse Proxy

A simple Node.js reverse proxy that forwards requests to running Docker containers based on subdomains.  

## Features
- Routes HTTP and WebSocket requests dynamically
- Management API to start containers

## Usage
Run with Docker Compose:

```bash
docker compose up
