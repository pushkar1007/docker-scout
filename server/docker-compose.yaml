version: "3.9"

services:
  docker_api_server:
    build: .
    container_name: docker_api_server
    ports:
      - "8089:8089"
    environment:
      - DOCKER_SCOUT_ADDR=:8089
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
